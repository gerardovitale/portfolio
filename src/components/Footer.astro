---
import type { SiteData } from "../data/site";
import type { Locale } from "../lib/locale";
import { localizeContentHref } from "../lib/routing";

interface Props {
  locale: Locale;
  siteData: SiteData;
}

const { locale, siteData } = Astro.props;
---

<footer class="footer">
  <div class="shell footer-grid">
    <div class="stack">
      <span class="eyebrow">{siteData.ui.footer.contactHeading}</span>
      <p class="footer-copy">{siteData.person.availability}</p>
      <a href={`mailto:${siteData.person.email}`}>{siteData.person.email}</a>
    </div>

    <div class="stack">
      <span class="eyebrow">{siteData.ui.footer.profilesHeading}</span>
      <div class="link-row">
        {
          siteData.person.socialLinks.map((link) => (
            <a
              href={localizeContentHref(link.href, locale, link.localized)}
              target={link.external ? "_blank" : undefined}
              rel={link.external ? "noreferrer noopener" : undefined}
            >
              {link.label}
            </a>
          ))
        }
      </div>
    </div>
  </div>
</footer>

<style>
  .footer {
    border-top: var(--line);
    padding: var(--space-7) 0 var(--space-6);
  }

  .footer-grid {
    display: grid;
    gap: var(--space-6);
  }

  .footer-copy {
    margin: 0;
    color: var(--color-text-muted);
    max-width: 34rem;
  }

  @media (min-width: 48rem) {
    .footer-grid {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
    }
  }
</style>
